# Unity Workspace

Unity-side code and scenes live here.

Current project root:

- `../OpenRDW`

Target monorepo layout:

- `unity/OpenRDW`

Keep Unity-specific generated folders, packages, scenes, prefabs, C# scripts, and Unity project settings inside the Unity workspace. Shared experiment data, trained models, and cross-runtime contracts should live under `../shared`.

